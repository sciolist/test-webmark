version: "3"
services:
  api:
    depends_on: [db]
    build: "${WEBMARK_DOCKER_ROOT}/configurations/${CONFIGURATION}"
    environment:
      RUST_BACKTRACE: 1
    ports:
      - 3000:3000
  db:
    build: "${WEBMARK_DOCKER_ROOT}/database"
    environment:
      POSTGRES_USER: "app"
      POSTGRES_PASSWORD: "app"
  adminer:
    image: adminer
    restart: always
    ports:
      - 8888:8080